@model IEnumerable<Services.ViewModels.ReservationModel>

@{
    ViewBag.Title = "Booking";
}


<div class="form-horizontal">
    <hr />
    @*<button type="button" class='btn btn-dark' name="btnLink" id="btnLink">New</button>*@

    <div class="form-group">
        <table class="table table-responsive" style="width:100%">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.ReservedId)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.CustomerName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ReservedBy)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ReservationDate)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.TripId)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.TripName)
                </th>
                <th>
                    Edit
                </th>
                <th>
                    Edit
                </th>
            </tr>
            @foreach (var item in Model)
            {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ReservedId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CustomerName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReservedBy)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReservationDate)
            </td>
            <td>

                @Html.DisplayFor(modelItem => item.TripId)
            </td>
            <td>

                @Html.DisplayFor(modelItem => item.TripName)
            </td>
            <td>
                <a class="btn btn-info">Edit</a>
            </td>
            <td>
                <a href='#' class='btn btn-danger'>Delete</a>
            </td>
        </tr>
            }
        </table>
    </div>

</div>

@*<div class="modal fade" id="ModalPopUp" role="dialog">

    <div class="modal-dialog err-pop" style="">
        <div class="modal-content">
            <div class="modal-header">
                <button id="DivClose" type="button" class="close" data-dismiss="modal">&times;</button>
            </div>
            <div class="modal-body" style="text-align:center;">

             
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal" asp-action="Create">Close</button>
                <button type="button" class="btn btn-primary">Save changes</button>
            </div>
        </div>
    </div>
</div>*@
